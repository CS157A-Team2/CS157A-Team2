
<!DOCTYPE html>
<html lang="en">
<head>
    <% include ../partials/head %>
</head>
<body class="container">

<header>
    <% include ../partials/header %>
</header>

<main>
    <div class="jumbotron">      
          <% if(firebase.auth().currentUser != null){ %>
        <% include ../partials/favorite %>
        <% } %>
        <h2><%=currentUser.currentContent.title + ", issue #" + currentUser.currentContent.issueNum %></h2>
        <h3><i><%= "Published on: " + currentUser.currentContent.publish_date.toString().substring(0,15) %></i></h3>
        <a href="/files/<%= currentUser.currentContent.content_id %>.txt" download>Download</a>
</main>

<footer>
    <% include ../partials/footer %>
</footer>

</body>
</html>
